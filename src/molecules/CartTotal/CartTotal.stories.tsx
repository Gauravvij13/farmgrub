import React from 'react';
import { CartTotal } from '.';

export default {
  title: `molecules/Cart/CartTotal`,
};
export const Carttotal = () => <CartTotal id="cart.total.merchandise" amount={100} />;
